---
const projects = [
  {
    title: "Eccos Beauty",
    image: "/public/eccosbeauty.jpg", // Cambia por la URL de tu imagen
    link: "https://eccosbeauty.com/"
  },
  {
    title: "Digital-Intech",
    image: "/public/digital-intech.jpg",
    link: "https://proyecto2.com"
  }
  

];
---

<section class="py-16 bg-gray-100" id="porta">
  <div class="container mx-auto px-6">
    <h2 class="text-3xl font-bold text-center text-gray-900">Mi Portafolio</h2>
    <p class="text-center text-gray-600 mt-2">Algunos de los proyectos en los que he trabajado.</p>

    <div class="mt-8 grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
      {projects.map((project) => (
        <a href={project.link} target="_blank" class="block bg-white rounded-lg shadow-lg overflow-hidden hover:shadow-2xl transform hover:-translate-y-2 transition duration-300">
          <img src={project.image} alt={project.title} class="w-full h-48 object-cover">
          <div class="p-4">
            <h3 class="text-lg font-semibold text-gray-800">{project.title}</h3>
          </div>
        </a>
      ))}
    </div>
  </div>
</section>
